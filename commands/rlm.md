---
name: rlm
description: Process large contexts using the RLM (Recursive LLM) agent — chunking, sub-querying, and synthesizing results iteratively.
allowed-tools:
  - Agent
---

Use the `rlm` agent to handle the following request:

$ARGUMENTS
